============
Installation
============

At the command line::

    $ easy_install django-model-info

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-model-info
    $ pip install django-model-info
